version: "3.9"

services:
  web:
    build: .
    container_name: business_management_system
    ports:
      - "8000:8000"
    env_file:
      - .env.docker
    volumes:
      - sqlite_data:/project/data
    restart: unless-stopped

volumes:
  sqlite_data: